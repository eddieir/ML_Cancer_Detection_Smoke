# Exact versions this project's CI is tested against: Python 3.11.15,
# pytest 9.1.1, scikit-learn 1.9.0, celltypist 1.7.1. CellTypist's published
# Immune_All_Low.pkl pretrained model was serialized with scikit-learn
# 0.24.1 and emits scikit-learn's InconsistentVersionWarning when unpickled
# under 1.9.0 — a disclosed, unresolved upstream compatibility gap this
# project does not control (see data/transforms.py::annotate_cell_types and
# CellTypistCompatibilityError). No newer CellTypist-published model
# resklearn'd against a matching scikit-learn version has been identified.
torch>=2.2.0
scanpy>=1.9.8
anndata>=0.10.0
harmonypy>=0.0.9
celltypist>=1.6.0
pybiomart>=0.2.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
pyyaml>=6.0
matplotlib>=3.8.0
seaborn>=0.13.0
umap-learn>=0.5.6
requests>=2.31.0
jupyter>=1.0.0